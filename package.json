{
  "name": "nina",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "migrate": "bun ./migrate/Migrate.ts",
    "start": "bun --env-file.env index.ts",
    "api:start": "bun --env-file.env server.ts",
    "api:watch": "bun --env-file.env --watch server.ts",
    "test": "bun --env-file=.env ./tests/index.ts",
    "node": "node --env-file=.env server.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "moment-timezone": "^0.5.45"
  }
}